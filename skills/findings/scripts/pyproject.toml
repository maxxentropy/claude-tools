[project]
name = "findings-scripts"
version = "1.0.0"
description = "Persistent findings storage for Claude Code"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
test = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

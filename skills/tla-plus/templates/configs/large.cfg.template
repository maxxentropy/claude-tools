\* TLA+ Model Configuration: Large (Thorough Verification)
\*
\* Use for: Pre-merge verification, release qualification
\* Expected: ~100,000-1,000,000 states, completes in minutes
\*
\* Generated by: tla-plus skill
\* Model: {{MODEL_NAME}}

\* --- Constants ---
CONSTANTS
    {{ENTITY_SET}} = {E1, E2, E3}
    {{ACTOR_SET}} = {A1, A2, A3}
    {{#if MAX_VERSION}}
    MAX_VERSION = 10
    {{/if}}
    {{#if ADDITIONAL_CONSTANTS}}
    {{ADDITIONAL_CONSTANTS}}
    {{/if}}

\* --- Specification ---
INIT Init
NEXT Next

\* --- Safety Properties ---
INVARIANTS
    {{#each INVARIANTS}}
    {{this}}
    {{/each}}
    {{#unless INVARIANTS}}
    Safety
    {{/unless}}

\* --- Liveness Properties (optional) ---
\* PROPERTIES
\*     EventuallyProcessed

\* --- Symmetry (optimization) ---
\* Symmetry can significantly reduce state space
\* SYMMETRY symmetry_{{ENTITY_SET}}
\* SYMMETRY symmetry_{{ACTOR_SET}}

\* --- Deadlock ---
\* CHECK_DEADLOCK TRUE

\* TLA+ Model Configuration: Small (Quick Verification)
\*
\* Use for: Development, CI simulation mode, quick feedback
\* Expected: ~1,000-10,000 states, completes in seconds
\*
\* Generated by: tla-plus skill
\* Model: {{MODEL_NAME}}

\* --- Constants ---
CONSTANTS
    {{ENTITY_SET}} = {E1, E2}
    {{ACTOR_SET}} = {A1, A2}
    {{#if MAX_VERSION}}
    MAX_VERSION = 5
    {{/if}}
    {{#if ADDITIONAL_CONSTANTS}}
    {{ADDITIONAL_CONSTANTS}}
    {{/if}}

\* --- Specification ---
INIT Init
NEXT Next

\* --- Safety Properties ---
INVARIANTS
    {{#each INVARIANTS}}
    {{this}}
    {{/each}}
    {{#unless INVARIANTS}}
    Safety
    {{/unless}}

\* --- Symmetry (optimization) ---
\* SYMMETRY symmetry_{{ENTITY_SET}}
\* SYMMETRY symmetry_{{ACTOR_SET}}

\* --- Deadlock ---
\* CHECK_DEADLOCK TRUE
